{% extends 'base.html.twig' %}

{% block title %}New Content{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between">
    <h3>Ajout des ingrédients</h1>
        <a class="btn btn-secondary" href="{{ path('app_back_recipe_index') }}">Retour à la liste</a>
    </div>
    <div class="mt-2">
        {{ include('back/content/_form.html.twig') }}
    </div>
{% endblock %}
