{% extends 'base.html.twig' %}

{% block title %}Modification : {{ vegetable.title }}{% endblock %}

{% block body %}
    <h1>Modification : {{ vegetable.title }}</h1>

    {{ include('back/vegetable/_form.html.twig', {'button_label': 'Update'}) }}
    
    <a class="btn btn-secondary" href="{{ path('app_back_vegetable_index') }}">Liste des fruits et légumes</a>
    
    {{ include('back/vegetable/_delete_form.html.twig') }}

{% endblock %}