<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="{{ asset('css/app.css')}}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@300;400;700&display=swap" rel="stylesheet">

    <title>{% block title %}Backoffice Seasonly{% endblock %}</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a href="{{ path("app_back_home") }}" class="navbar-brand d-flex">
            <img src="{{ asset('images/seasonly4.png')}}" height="60" alt="Logo de Seasonly">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path("app_back_home") }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_back_vegetable_index") }}">Vegetable</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_back_recipe_index") }}">Recipe</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_back_ingredient_index") }}">Ingredient</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_back_member_index") }}">Member</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_back_user_index") }}">User</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" style="color: white;">
                        <span>{{ app.user.pseudo }}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="{{ path('app_back_logout') }}">Déconnexion</a></li>
                    </ul>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>


    <div class="container pt-5">
        <div class="row mb-2">
            <div class="col">
                <div class="" role="alert">
                </div>

                {% block body %}
                {# C'est ici qu'on va coder nos vue #}
                {% endblock %}
            </div>
        </div>
    </div>

    <footer>
        <div class="text-center mb-4">
            © 2023 Copyright:
            <a class="text-reset fw-bold" href="{{ path("app_back_home") }}">Seasonly</a>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-/bQdsTh/da6pkI1MST/rWKFNjaCP5gBSY4sEBT38Q/9RBh9AH40zEOg7Hlq2THRZ"
        crossorigin="anonymous"></script>
</body>

</html>
